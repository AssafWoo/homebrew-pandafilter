[package]
name = "panda-core"
version = "0.1.0"
edition = "2021"

[dependencies]
regex.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
anyhow.workspace = true
thiserror.workspace = true
bpe-openai.workspace = true
once_cell = "1"
libc = "0.2"
ort = { version = "=2.0.0-rc.12", features = ["std", "ndarray", "download-binaries", "tls-native"], default-features = false }
tokenizers = { version = "0.21", features = ["onig"], default-features = false }
ndarray = "0.17"
hf-hub = { version = "0.4", features = ["ureq", "native-tls"] }
rusqlite = { version = "0.31", features = ["bundled"] }
walkdir = "2"

[dev-dependencies]
tempfile = "3"
# Kept as a dev-dependency only: parity tests verify bpe-openai produces
# identical cl100k token counts to the previous tiktoken-rs implementation.
tiktoken-rs.workspace = true
